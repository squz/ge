build/
bin/
*.o
*.passed
# clangd compile database — generated by `make compile_commands.json`
# (root and per-sample). Machine-local; never committed.
compile_commands.json
math_bench.md
ged/ged
ged/web/
/.claude/*
!/.claude/skills/
!/.claude/agents/
!/.claude/settings.json
!/.claude/CLAUDE.md
web/sqlite_mcp_server.db
# Generated per-sample distribution projects (regenerated from templates
# via `make ge/ios-init` / `make ge/android-init`). Do not hand-edit.
/sample/*/ios/
/sample/*/android/
/sample/*/build-server/

# fastlane (🎯T64.1): per-machine bundler artefacts; Gemfile.lock is
# committed so fastlane versions stay aligned across the studio, but the
# install dir and run-time reports are machine-local.
.bundle/
vendor/bundle/
/fastlane/report.xml
/fastlane/Preview.html
/fastlane/screenshots/
/fastlane/test_output/

# Playwright MCP browser session artefacts
.playwright-mcp/
asc-keys-page.png
