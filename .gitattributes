# Prebuilt archives are local-only (not committed). See docs/vendor-prebuilds.md.
# cook.json / manifest.json remain plain git if present under prebuilt/.
prebuilt/**/cook.json text
prebuilt/**/manifest.json text
